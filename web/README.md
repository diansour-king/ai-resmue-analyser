Phase 2. The Next.js application, ported from the Stitch export in /design.

/design is not yet in the repository. Section 5 of the build spec is blocked until it is:
the API response shapes are derived from what the screens need, not the other way round.
